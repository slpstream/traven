---
"@freedomware/traven": patch
---

Implement configurable HTML sanitization option (`sanitizeHtml`) in the editor options and automatic dynamic global `window.DOMPurify` detection in `getContentHtml()` to secure raw HTML previews.
