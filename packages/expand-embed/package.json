{
  "name": "@freedomware/traven-expand-embed",
  "version": "1.0.0",
  "description": "Traven plugin: [[wikilinks]] with expand / embed transclusion",
  "license": "MIT",
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/expand-embed.js",
  "module": "dist/expand-embed.js",
  "files": [
    "dist",
    "assets"
  ],
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=esm --outfile=dist/expand-embed.js --external:@freedomware/traven && esbuild src/runtime.js --bundle --format=esm --outfile=dist/expand-embed-runtime.js && cp assets/expand-embed.css dist/expand-embed.css",
    "test": "vitest run"
  },
  "peerDependencies": {
    "@freedomware/traven": ">=0.3.0"
  },
  "devDependencies": {
    "@freedomware/traven": "*",
    "esbuild": "^0.28.1",
    "jsdom": "^24.0.0",
    "vitest": "^4.1.11"
  }
}
